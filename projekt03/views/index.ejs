<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Menedżer Zadań</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <header>
      <h1>Menedżer Zadań</h1>
      <nav>
        <a href="/">Strona główna</a> |
        <a href="/add">Dodaj nowe zadanie</a> |
        <a href="/about">O projekcie</a>
      </nav>
    </header>

    <main>
      <section class="tasks-section">
        <h2>Moje zadania</h2>
        
        <% if (tasks && tasks.length > 0) { %>
          <ul class="tasks-list">
            <% tasks.forEach(task => { %>
              <li class="task-item <%= task.completed ? 'completed' : '' %>">
                <div class="task-content">
                  <h3><%= task.title %></h3>
                  <% if (task.description) { %>
                    <p><%= task.description %></p>
                  <% } %>
                  <small>Dodane: <%= new Date(task.created_at).toLocaleDateString('pl-PL') %></small>
                </div>
                <div class="task-actions">
                  <a href="/edit/<%= task.id %>" class="btn btn-edit">Edytuj</a>
                  <form action="/delete/<%= task.id %>" method="post" style="display: inline;">
                    <button type="submit" class="btn btn-delete" onclick="return confirm('Czy na pewno chcesz usunąć to zadanie?')">Usuń</button>
                  </form>
                </div>
              </li>
            <% }) %>
          </ul>
        <% } else { %>
          <p class="empty-state">Brak zadań. <a href="/add">Dodaj pierwsze zadanie!</a></p>
        <% } %>
      </section>
    </main>

    <footer>
      <p>&copy; 2025 Aplikacja zarządzania zadaniami | Express + SQLite</p>
    </footer>
  </div>
</body>
</html>
